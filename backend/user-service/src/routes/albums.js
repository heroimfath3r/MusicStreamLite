// src/routes/albums.js
import express from 'express';
const router = express.Router();

// Placeholder (para futuro)
router.get('/', (req, res) => {
  res.json({ message: 'Albums endpoint ready' });
});

export default router;
